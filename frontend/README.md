# Frontend

Next.js App Router + TypeScript scaffold for the UV LED Knowledge Platform.

## Setup
1. Install dependencies:
   - `npm install`
2. Create `.env.local` with:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`

## Run
```
npm run dev
```
