# Database Setup

1. Open the Supabase SQL editor for your project.
2. Run `schema.sql` to create tables and indexes.

Tables created:
- `research_items`
- `hot_topics`
- `benchmarks`
- `forum_messages`
- `forum_replies`
